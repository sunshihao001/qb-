# Context Budget Policy V2.0

上下文不是仓库，而是工作内存。装配顺序：control_plane → active_thread_state → acceptance_criteria → evidence → current_errors → recent_tool_results → memory_revalidation → historical_summary。历史噪声、未验证记忆、过期材料必须摘要或排除。compact 是语义重建，不是历史摘要。
