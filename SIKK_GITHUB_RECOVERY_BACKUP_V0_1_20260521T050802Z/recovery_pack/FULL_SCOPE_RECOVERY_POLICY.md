# FULL_SCOPE_RECOVERY_POLICY

This is a non-secret portable recovery pack. It includes source-of-truth control-plane artifacts and selected non-secret run evidence.

Never include: `.env`, API tokens, private keys, wallet secrets, signing material, cookies, browser sessions, live trading credentials.

Secrets must be restored externally by the user after verification.
