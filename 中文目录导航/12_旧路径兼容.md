# 旧路径兼容

- 中文名：旧路径兼容
- 真实路径：`/root/sikk-gmgn/legacy_compat/`
- 用途：manifest、path map、read fallback；记录旧路径到新路径映射。
- 重要边界：只放索引，不放大数据；迁移只能 copy-only。

## 常见文件 / 子目录

- `manifests/<manifest_name>.json`：legacy manifest。
- `path_maps/<map_name>.json`：旧路径到新路径映射。
- `read_fallbacks/<fallback_name>.md`：兼容读取说明。

## 是否主写路径

是，但必须按对应资产类型和子目录写入。