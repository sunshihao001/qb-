# 结构推断数据

- 中文名：结构推断数据
- 真实路径：`/root/sikk-gmgn/data/intel_bot/`
- 用途：主导侧行为动机推断、反证、量化分、结构结论。
- 重要边界：只能读取钱包事实 handoff/manifest；不能直接读 paper/dashboard/state_machine。

## 常见文件 / 子目录

- `behavior_inference/dominant_behavior_inference.json`：主导侧行为动机推断主结果。
- `behavior_inference/chip_control_status.json`：筹码控制状态。
- `reports/behavior_reasoning_report.md`：行为推断解释报告。
- `counter_evidence/*.json|md`：反证输出。
- `quant_scores/*.json`：量化分。
- `manifest/token_output_manifest.json`：Intel 输出包索引。

## 是否主写路径

是，但必须按对应资产类型和子目录写入。