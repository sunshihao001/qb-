# 运行数据

- 中文名：运行数据
- 真实路径：`/root/sikk-gmgn/data/`
- 用途：所有运行输出与标准化数据；程序主读写数据根。
- 重要边界：不要直接把新 token 输出写到 data/ 根；必须进入 data/<bot>/<mode>/<asset_id>/。

## 常见文件 / 子目录

- `<按实际子目录查看>`：详见总览和 system_directory_usage_map_for_decision_20260506.md。

## 是否主写路径

是，但必须按对应资产类型和子目录写入。