# 方法论资产库

- 中文名：方法论资产库
- 真实路径：`/root/sikk-gmgn/research_loop/methodology/`
- 用途：方法护照、判断规则、反证规则、统计模型、字段映射、审计规则。
- 重要边界：方法论不要散放 docs 或 token 运行目录。

## 常见文件 / 子目录

- `passports/<source_id>_passport.md|json`：方法护照。
- `rules/<rule_family>.md|json`：判断规则。
- `counter_evidence/<counter_rule>.md|json`：反证规则。
- `stat_models/<model_name>.md|json`：统计/量化模型。
- `field_maps/<map_name>.md|json|csv`：字段映射。
- `audit_rules/<audit_name>.md|json`：审计规则。

## 是否主写路径

是，但必须按对应资产类型和子目录写入。