# Minimal restore/runtime dependencies for SIKK-GMGN backup restore.
# Purpose: make a fresh restored checkout able to run restore validation and core safety tests.
# This is intentionally small: do not add trading/API credentials here.

pytest>=8.0,<10.0
PyYAML>=6.0,<7.0
jsonschema>=4.0,<5.0
python-telegram-bot>=20.0,<22.0
