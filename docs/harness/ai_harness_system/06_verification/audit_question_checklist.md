# 审计问题清单

```text
是否只是写了文档，没有接入流程？
是否只是建了目录，没有真实输出？
是否只是生成字段，没有数据来源？
是否只是写了命令，没有运行验证？
是否只是模型声称完成，没有证据？
是否把临时规则写成长期规则？
是否覆盖了旧数据？
是否有失败恢复路径？
```

## 使用方式

每次任务完成前必须逐项检查。

任一问题答案为“是”，都不能直接宣布完成，必须进入验证补充或恢复路径。
