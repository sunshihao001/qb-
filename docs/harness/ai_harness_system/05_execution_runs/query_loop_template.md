# Query Loop 模板

```text
while 任务未完成:
    读取当前任务状态
    读取相关上下文
    检查上下文是否过载
    生成本轮最小执行单元
    执行本轮动作
    记录输出
    判断是否成功
    如果失败，进入恢复路径
    如果成功，进入验证路径
    更新任务状态
```

## 每一轮必须输出

```text
本轮目标
本轮输入
本轮执行动作
本轮输出文件
本轮验证结果
本轮问题
下一轮动作
```
