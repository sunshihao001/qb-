本次认知升级点

1. P01 不是数据文档，而是事实入口控制器。
2. GMGN / OKX 接入属于 P01 核心成立条件。
3. **P01 的核心产物不是 raw 数据，而是 data_fact_handoff_packet.json。**下游必须被 P01 授权读取，不能各自乱读接口输出。出。**缺字段、旧字段、空返回、接口失败都必须状态化。化。**专业化不是把模块堆多，而是每个字段都有来源、时间、置信度、缺失原因、下游权限。限。**P01 完成前，策略层继续优化没有意义。义。**

---
