# Governance Plane 治理平面

状态：CREATED_BY_P00  
生成时间：2026-05-11T15:11:51Z  
安全边界：paper-only，real_trade_enabled=false

## 核心治理规则

- P00 负责系统建造与阶段注册，不做交易判断。
- P01 在 Data Plane 验收前必须保持 `BLOCKED_BY_DATA_PLANE`。
- 策略层不得读取未标准化原始数据直接裁决。
- 执行层不得反向污染分析层。
- 复盘结果不得未经 P10 直接修改实时规则。
- 禁止真实交易、签名、广播、私钥、seed phrase、swap/order API 写入。
- 每个阶段必须通过 acceptance gate，不能用文件存在代替语义落实。
