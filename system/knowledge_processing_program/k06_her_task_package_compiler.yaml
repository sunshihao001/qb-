phase_id: K06
phase_name: "HER Task Package Compiler"
program: KNOWLEDGE_PROCESSING_PROGRAM
status: CONTROLLER_CANDIDATE_READY
purpose: "compile schedulable HER task package with objective, inputs, outputs, acceptance, forbidden actions, handoff"
required_inputs:
  - upstream_handoff_or_raw_document
required_outputs:
  - her_task_package.yaml
acceptance:
  - outputs_exist
  - source_trace_present
  - io_contract_declared
  - handoff_declared
forbidden:
  - production_mutation_without_governance
  - summary_only_completion
  - untraced_claims
