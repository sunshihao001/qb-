# Knowledge Processing Program Context

本体系不是文档总结器，而是 HER 上游知识功能化程序。

核心规则：文档进入 HER 后必须先保存、登记、分类、切块、抽取功能对象、映射系统层、生成候选、编译任务包、验收、handoff、索引。解释性文本不能直接成为生产规则；单篇文档只能生成 candidate，并等待 P10/Governance 审查。

禁止：只总结、直接执行、直接改生产系统、无 trace、无 acceptance、无 handoff、无 I/O 对象。
