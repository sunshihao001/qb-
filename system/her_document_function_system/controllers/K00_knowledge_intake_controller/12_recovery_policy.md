# K00 Recovery Policy

- 缺 raw source：阻断，要求重新提供 source path/material。
- 缺 passport/index/mapping/gap：不得进入 F00，先补 K00 派生产物。
- 缺 repo_root/write_policy：降级 DESIGN_ONLY。
- 缺 KV：记录 KV_GAP，可继续。
- 任何恢复必须保留原始缺口，不得把 `READY_WITH_GAPS` 改成 `READY`。
