# O00 Context Pack

O00_RUN_DOCUMENT is the safe-mode command/orchestrator entry for the HER document-to-function system. It converts a user document and operator goal into a gated pipeline run, evidence bundle, gap register, acceptance result, trace/audit logs, and final report.

Boundary: O00 may create design-level or safe-mode replay evidence only. It must not claim real tests, runner binding, governance activation, live runtime, wallet signing, auto deploy, production trading, or full system implementation unless downstream evidence exists.
